
<CH.Code>
```typescript UpdateVisibleAccountsSyncFlow.ts
import { CodatSyncCommerce } from "@codat/sync-for-commerce";

async function run() {
  const sdk = new CodatSyncCommerce({
    security: {
      authHeader: "Basic BASE_64_ENCODED(API_KEY)",
    },
  });

  const result = await sdk.syncFlowSettings.updateVisibleAccountsSyncFlow({
    visibleAccounts: {
      visibleAccounts: [
        "string",
      ],
    },
    platformKey: "gbol",
  });

  // Handle the result
  console.log(result)
}

run();
```
---

```json Example Response
  {
    "canBeRetried": "string",
    "correlationId": "string",
    "detailedErrorCode": 602763,
    "error": "string",
    "service": "string",
    "statusCode": 857946
  }
```
</CH.Code>
